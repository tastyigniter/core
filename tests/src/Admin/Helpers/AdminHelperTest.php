<?php

namespace Tests\Admin\Helpers;

it('returns ajax handler from request header')->skip();

it('validates ajax handler with no errors')->skip();

it('validates ajax handler with errors')->skip();

it('validates ajax handler partials with no errors')->skip();

it('validates ajax handler partials with errors')->skip();